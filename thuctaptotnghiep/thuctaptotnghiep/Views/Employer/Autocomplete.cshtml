@model IEnumerable<Model.Framework.KyNang>
           @if (Model.Count() <= 0)
           {
              <option></option>
           }
           else
           {
               foreach (var item in Model)
               {
                <option value="@item.TenKN">@item.TenKN</option>
               }

           }
